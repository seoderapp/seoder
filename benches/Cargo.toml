[package]
name = "benches"
version = "0.0.0"
publish = false
edition = "2021"

[dependencies]
seoder_lib = { path = "../seoder_lib" }
criterion = { version = "0.3.5", features = ["html_reports", "async_tokio"] }

[[bench]]
name = "crawl"
path = "crawl.rs"
harness = false