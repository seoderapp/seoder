---
import AnchorButton from "./AnchorButton.astro";
import Search from "./Svgs/Search.astro";
import Download from "./Download.astro";
import Title from "./Typo/Title.astro";
import Section from "./Section.astro";
import FeatureList from "./FeatureList.astro";
import Hero from "./Svgs/Hero.astro";
import Highlight from "./Highlight.astro";
import { appTitle } from "../statics/title";

const canonicalURL = new URL(Astro.site);
---

<div class="prog">
  <Section>
    <div class="flex-wrap">
      <div>
        <Title>{appTitle}</Title>
        <p class="instructions text-primary">
          Import thousands of domains and identify organic meta information {
            `you’re`
          } looking for.
        </p>
        <div>
          <AnchorButton
            href={`${canonicalURL}payments`}
            title={"Try Seoder - It's free!"}
          />
          <p class="trial">Get started now with a free 7 day trial</p>
        </div>
      </div>
      <div>
        <Hero />
      </div>
    </div>
  </Section>
  <Section>
    <div class="row features">
      <div class="features">
        <Highlight>Features</Highlight>
        <h2 class="text-primary sub-heading">Beyond fast keyword finder</h2>
        <Search />
      </div>
      <FeatureList />
    </div>
  </Section>
  <Section center>
    <video controls width={960} autoplay={false} class="border" poster="/media/seoder_placeholder.png">  
      <source src="/media/seoder_main.mp4" type="video/mp4">
      Download the
      <a href="/media/seoder_main.webm">WEBM</a>
      or
      <a href="/media/seoder_main.mp4">MP4</a>
      video.
    </video>
  </Section>
  <Section>
    <Download />
  </Section>
  <Section>
    <div class="bubble">
      <h4 class="steps-header text-primary">
        Seoder steps for keyword matching domains
      </h4>
      <p class="steps text-primary">
        It takes 3 steps to enhance your next marketing campaign with Seoder.
        First you establish your target domains in a list or standard txt file.
        Then determine the keywords you are looking for across the list. The
        last step is running the campaign that will provide the intelligence to
        determine if the website matches the category. After it finds the match
        it delivers information to help provide follow-ups and other workflows.
        You can think of Seoder as your personal engine to determine keywords
        and SEO targets for websites with entry secure public contact
        information.
      </p>
    </div>
  </Section>
</div>

<style>
  .heading {
    font-size: 1.4rem;
  }
  .sub-heading {
    font-size: 2.25rem;
    max-width: 14rem;
    font-weight: 600;
  }
  .trial {
    font-size: 0.85rem;
    color: #2f2768;
    padding: 0.6rem;
    margin: 0;
    opacity: 0.85;
  }
  .text-center {
    text-align: center;
  }
  .border {
    border-radius: 0.4rem;
    border: 6px solid #2f2768;
  }
  .center {
    text-align: center;
  }
  .row {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 3rem;
  }
  .features {
    justify-content: center;
    flex: 0.4;
  }
  .text-primary {
    color: #2f2768;
  }
  .instructions {
    font-size: 2rem;
    max-width: 36rem;
    color: #2f2768;
    margin: 0;
    padding-bottom: 3.6rem;
  }
  .bg {
    width: 100%;
    height: auto;
  }
  .flex-wrap {
    display: flex;
    flex-direction: row;
    gap: 5.9rem;
    text-align: left;
  }
  .bubble {
    border: 1px solid #2f2768;
    border-radius: 1rem;
    padding: 1rem 2rem;
  }
  .steps-header {
    font-size: 1.4rem;
    font-weight: 500;
  }
  .steps {
    line-height: 1.8rem;
    font-size: 1rem;
  }
  @media only screen and (max-width: 1080px) {
    .flex-wrap {
      gap: 3rem;
      flex-wrap: wrap;
    }
  }
</style>
